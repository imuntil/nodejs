const ct = require('./ct')
const PM = require('./permission')
const credentials = require('./credentials')
const sendMail = require('./email')

module.exports = {
  ct,
  PM,
  credentials,
  sendMail
}